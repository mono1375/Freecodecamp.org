// SetUp funcion to define the parseInt
function convertToInteger(str) {
// Creat a variable  and assing the str in the parseInt 
    var x = parseInt(str);
  // return the veriable 
    return x  
  
  }
  // adding the str variable to the funcion
  convertToInteger("56");